spring:
  application:
    name: permissions-manager
  datasource:
    url: jdbc:postgresql://localhost:5432/permsdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
server:
  port: 8083